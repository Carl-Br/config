local options = {
  ensure_installed = {"elixir","heex", "lua", "vim", "vimdoc", "java" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
